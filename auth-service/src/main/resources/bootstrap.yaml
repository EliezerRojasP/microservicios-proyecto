spring:
  application:
    name: auth-service
  cloud:
    config:
      enabled: true
      uri: http://localhost:8081

jwt:
  secret: "${JWT_SECRET:defaultSecret}"
  expiration: 3600000

           

